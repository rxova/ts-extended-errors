{
  "name": "@repo/tooling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Repo scripts: the verify gate, pack-smoke, check-llms and check-changeset.",
  "scripts": {
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "^26.6.1",
    "@vitest/coverage-v8": "^5.0.1",
    "tsx": "^4.23.13",
    "typescript": "6.0.3",
    "vitest": "^5.0.1"
  }
}
