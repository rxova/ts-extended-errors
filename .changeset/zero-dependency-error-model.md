---
'ts-extended-errors': patch
---

Describe the package as a zero-dependency error model for TypeScript applications that use native exceptions but need typed context, cause-chain inspection, and reliable JSON round trips. The npm description, README and `llms.txt` summary change; the code does not.
