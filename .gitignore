node_modules
dist
# The vendored builds are the one dist that is source.
!/vendor/*/dist/
build
coverage
.turbo
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example

# Local agent state. The Stop hooks are the exception: cloud and mobile sessions
# read hooks from the repository, never from ~/.claude.
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude.json
CLAUDE.local.md

# Run records written by the integrity tools.
.overlock/
.basting/
.saidso/
